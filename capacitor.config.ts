import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Instagram-Messages-Focus-App',
  webDir: 'www'
};

export default config;
